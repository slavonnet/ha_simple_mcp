"""Namespace package marker for Home Assistant custom components.

This file prevents type-checking tools from collapsing
`custom_components/ha_simple_mcp` into the top-level `ha_simple_mcp`
module name, which conflicts with the reusable package in `src/`.
"""
